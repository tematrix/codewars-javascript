/*
DESCRIPTION:
Complete the solution so that it takes the object (JavaScript/CoffeeScript) 
or hash (ruby) passed in and generates a human readable string from its key/value pairs.

The format should be "KEY = VALUE". Each key/value pair 
should be separated by a comma except for the last pair.

Example:

solution({a: 1, b: '2'}) // should return "a = 1,b = 2"


ПЕРЕВОД:
Завершите решение так, чтобы оно принимало переданный объект (JavaScript/CoffeeScript) 
или хэш (Ruby) и генерировало строку, читаемую человеком, из его пар ключ/значение.

Формат должен быть "KEY = VALUE". Каждая пара ключ/значение 
должна быть разделена запятой, за исключением последней пары.

Пример:

solution({a: 1, b: '2'}) // должно вернуть "a = 1, b = 2"
*/