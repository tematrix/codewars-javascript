/*
DESCRIPTION:
Oh no, our Math object was "accidently" reset. Can you re-implement some of those functions? 
We can assure, that only non-negative numbers are passed as arguments. 
So you don't have to consider things like undefined, null, NaN, negative numbers, strings and so on.

Here is a list of functions, we need:

Math.round()
Math.ceil()
Math.floor()


ПЕРЕВОД:
О нет, наш объект Math был "случайно" сброшен. Можете ли вы повторно реализовать некоторые из этих функций? 
Мы можем гарантировать, что в качестве аргументов передаются только неотрицательные числа. 
Поэтому вам не придется рассматривать такие вещи, как undefined, null, NaN, отрицательные числа, строки и так далее.

Вот список функций, которые нам нужны:

Math.round()
Math.ceil()
Math.floor()
*/